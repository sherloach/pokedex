import { HeroSection, PokedexList, SearchBar } from '@/components';

const Home = () => {
  return (
    <>
      <HeroSection />
      <SearchBar />
      <PokedexList />
    </>
  );
};

export default Home;
